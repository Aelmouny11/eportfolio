# Lycée Qualifiant Mohammed 6 - Marrakech

import Image from "next/image";
import Link from "next/link";
import { useState } from "react";
import { HiLocationMarker, HiGlobe } from "react-icons/hi";

{

<div class="space-y-1">
	<div class="flex items-center gap-4">
		<HiLocationMarker size={24} className="text-blue-500" />
		Av. Mohammadia, Marrakech 40150
	</div>

    <div class="flex items-center gap-4">
    	<HiGlobe size={24} className="text-blue-500" />
    	<Link href="http://l-mohammedvi.marrakech.men.gov.ma/">http://l-mohammedvi.marrakech.men.gov.ma/</Link>
    </div>

</div>
}

![Lycee](/lycee.jpg)

Le lycée M6 existe à Marrakech, c'est un lycée technique, il a donc des filières telles que :

-   Les fillières Téchnologique
-   Sciences Mathématique
-   BTS

---

export const CardGallery = () => {
const images=[{name:"sport",path:"/sport.jpg"},{name:"pavement",path:"/pavement.jpg"},{name:"lobby",path:"/lobby.jpg"},{name:"bts",path:"/bts.jpg"},{name:"entrance",path:"/entrance.jpg"}];
  const [selectedImage, setSelectedImage] = useState(null);

return (

<div className="flex justify-evenly gap-4 flex-wrap">
	{images.map((image, index) => (
		<Card
			img={`/iloveimg-compressed${image.path}`}
			alt={`Image ${index}`}
			text={image.name}
		/>
	))}
</div>
); };

## Highlights

<CardGallery />

export const Card = ({ img, text }) => {
	return (
		<div className="relative w-80 h-80 group ">
			<Image src={img} fill />
			<div className="w-full h-full absolute rounded-xl bg-slate-800/40 group-hover:flex hidden transition duration-700  items-center justify-center">
				{" "}
				<h1 className="text-blue-400 font-extrabold text-4xl uppercase">
					{text}
				</h1>
			</div>
		</div>
	);
};
 
## Cadres et enseignats
